# Course Contract Template

> **When to read this:** Before writing module briefs. Save the completed contract as
> `course-name/briefs/00-course-contract.md` and give it to every module-writing agent.

## Source Scope

- Repository and remote URL:
- Pinned commit ID and date:
- Default execution path taught:
- Optional paths shown, with their enabling configuration:
- Release/default changes that affect this course:

## Canonical Actor and Ownership Registry

| Canonical learner-facing name | Source name | Process or boundary | Logical responsibility | Physical resource location | Color/class |
|---|---|---|---|---|---|
| [actor] | [identifier] | [process/device/external] | [owns/decides] | [where bytes or hardware live] | [class] |

State explicitly when a component's logical owner, executing process, and physical
storage location differ.

## Shared Vocabulary and Units

| Canonical term | Visible first-use definition | Do not use as a synonym | Unit or ID domain |
|---|---|---|---|
| [term] | [definition] | [ambiguous alternatives] | [unit/domain] |

List course-only labels such as `R1`, `L0`, or `P7` with explicit antecedents. Put
literal commands, protocol strings, generated text, and method-name messages in double
quotation marks where they appear as data rather than code syntax.

## Shared Example State

- User-supplied input:
- Tokenized or converted state:
- Already computed or cached state:
- Work scheduled now:
- Newly selected, sampled, or emitted values:
- Values first processed or stored in the following step:
- Returned result:
- Screen or module boundaries where the example advances, branches, or resets:

## Visual and HTML Rules

- Actor colors and ownership-zone classes:
- Figure grammar and arrow conventions:
- Source-snippet and code-to-English pairing rules:
- Tooltip policy:
- Target desktop and mobile viewport widths:
- Module fragment constraints and prohibited inline styles/scripts:
- Build and validation commands:
